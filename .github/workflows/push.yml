name: triggered on pushes

on: [push]

jobs:
  build:
    name: ESLint
    
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - uses: hallee/eslint-action@63c06f1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
